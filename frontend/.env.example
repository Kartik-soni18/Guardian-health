# API Configuration
VITE_API_URL=http://localhost:8000/api

# Set to true to show the maintenance page instead of the full app (used in CI deploy)
# VITE_MAINTENANCE_MODE=false

# Optional: Analytics
# VITE_ANALYTICS_ID=
